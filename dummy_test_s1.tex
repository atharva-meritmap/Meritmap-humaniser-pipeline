\documentclass{article}
\usepackage{amsmath}

\begin{document}

\section{Introduction}
It is worth noting that we propose a groundbreaking novel method for neural networks using deep learning. 
At the present time, a large number of researchers are studying this topic \cite{smith2023}. 
Due to the fact that we use an advanced algorithm, the results are very good. 
It is observed that the method is fast. 
It is demonstrated that the accuracy is 92.5\%.

\section{Methodology}
In order to carry out an experiment, we train the ResNet50 model. 
The loss function is defined as:
\begin{equation}
L = \sum_{i=1}^{N} (y_i - \hat{y}_i)^2
\end{equation}
As a consequence of the training, we find that $p < 0.05$. 
The dataset used is ImageNet.

\section{Conclusion}
In spite of the fact that it is a complex problem, we solve it. 
It is clear that our method works. 
Future work will explore this more.

\end{document}
